# Exact versions used to produce the recorded Q4 run.
# Partner B: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
# (The assignment permits mamba, conda, OR virtualenv.)
mlflow==3.15.1
scikit-learn==1.9.0
numpy==2.5.2
pandas==2.3.3
dvc==3.64.0
